function home() {
    var targetSection = document.querySelector('#home');
    targetSection.scrollIntoView({ behavior: 'smooth' });
  }
  
  
  
  function about() {
      var targetSection = document.querySelector('#about');
      targetSection.scrollIntoView({ behavior: 'smooth' });
    }
    
  
    function skills() {
      var targetSection = document.querySelector('#skills');
      targetSection.scrollIntoView({ behavior: 'smooth' });
    }
    
    function contanct() {
      var targetSection = document.querySelector('#contanct');
      targetSection.scrollIntoView({ behavior: 'smooth' });
    }